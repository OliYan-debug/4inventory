# Use postgres/example user/password credentials
version: '3.9'

services:

  db:
    image: postgres
    restart: always
    shm_size: 128mb
    env_file: "postgres.env"

  adminer:
    image: adminer
    restart: always
    ports:
      - 8080:8080

